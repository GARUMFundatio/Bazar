<% if @mensaje.tipo == 'M' %>
	<h3 class="page-title"><%=t(:text_enviar_mensaje)%></h3>
<% else %>
	<h3 class="page-title"><%=t(:text_enviar_notificaci_n)%></h3>
<% end %>

<%= render 'form2' %>

<% if @mensaje.tipo == 'M' %>
	<%= link_to t(:label_volver_a_la_lista_de_mensajes),"/mensajes?tipo=M" %>
<% else %>
	<%= link_to t(:label_volver_a_la_lista_de_notificaciones),"/mensajes?tipo=N" %>
<% end %>

