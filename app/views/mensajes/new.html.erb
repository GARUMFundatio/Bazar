<% if @mensaje.tipo == 'M' %>
	<h3 class="page-title">Enviar Mensaje</h3>
<% else %>
	<h3 class="page-title">Enviar Notificación</h3>
<% end %>

<br />
<%= render 'form2' %>

<% if @mensaje.tipo == 'M' %>
	<%= link_to 'Volver a la lista de mensajes',"/mensajes?tipo=M" %>
<% else %>
	<%= link_to 'Volver a la lista de notificaciones',"/mensajes?tipo=N" %>
<% end %>

