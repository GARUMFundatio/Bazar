<% if @mensaje.tipo == 'M' %>
	<b>Enviar Mensaje a todos los Usuarios</b>
<% else %>
	<b>Enviar Notificación a todos los Usuarios</b>
<% end %>

<br />
<%= render 'form3' %>

<% if @mensaje.tipo == 'M' %>
	<%= link_to 'Volver a la lista de mensajes',"/mensajes?tipo=M" %>
<% else %>
	<%= link_to 'Volver a la lista de notificaciones',"/mensajes?tipo=N" %>
<% end %>