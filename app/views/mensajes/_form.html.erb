<%= form_for(@mensaje) do |f| %>

  <div class="field">
    <%= f.label :texto %><br />
    <%= f.text_area :texto, :size => "60x10" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
